# GoInsultGenerator
An insult generator written in Golang, Bootstrap and HTMX

Another stupied Golang experiment, I happen to be playing with Bootstrap and Golang HTML templates, plus a little bit of HTMX, needed something to get more practice and wrote this rubbish.

Compile it and it will embed the index.html file in the binary and so it can be moved around without need for external files.
 a "
Start the server, access it via a browser and hit the "Generate" button, also has a "Copy to clipboard" button for easy insulting!
